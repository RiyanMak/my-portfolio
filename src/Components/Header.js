import React from 'react';

const Header = () => {
    return(
        <header>
            <nav>
                <div className="logo">{
                    <a href="#">My Portfolio</a>
                }</div>
            </nav>
        </header>
        
    );
};

export default Header;